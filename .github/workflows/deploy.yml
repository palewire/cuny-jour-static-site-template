# GitHub Actions workflow to build and deploy to GitHub Pages
#
# HOW TO ENABLE:
# 1. Go to your repository Settings > Pages
# 2. Under "Source", select "GitHub Actions"
# 3. Uncomment all the lines below (remove the # at the start of each line)
# 4. Commit and push this file
# 5. Your site will build and deploy automatically to https://<your-github-username>.github.io/<your-repository-name>/

# name: Deploy to GitHub Pages
#
# on:
#   push:
#     branches: ['main']
#   workflow_dispatch:
#
# permissions:
#   contents: read
#   pages: write
#   id-token: write
#
# concurrency:
#   group: 'pages'
#   cancel-in-progress: true
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#
#       - name: Install dependencies
#         run: npm ci
#
#       - name: Build
#         run: npm run build
#         env:
#           BASE_PATH: /${{ github.event.repository.name }}
#
#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: 'build'
#
#   deploy:
#     needs: build
#     runs-on: ubuntu-latest
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4